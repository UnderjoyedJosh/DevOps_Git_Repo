pipeline {
    agent {
        label 'docker'
    }

    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                docker { 
                    label 'docker'
                    image 'python:latest'
                }
                echo 'Building..'
                sh 'python -m pip install -r requirements.txt'
                sh 'python tests.py'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
